# 🎴 策略卡片系統 (Strategy Card System)

## 概念

把交易參數變成可插拔的「超能力卡片」，系統會根據市場狀態自動選擇最適合的卡片組合。

## 卡片類型

### 1. 進場卡片 (Entry Cards)
- 決定什麼時候進場
- 六維評分門檻、OBI 過濾、動能確認等

### 2. 出場卡片 (Exit Cards)  
- 決定什麼時候出場
- 止盈止損、N%鎖N%、追蹤止盈等

### 3. 風控卡片 (Risk Cards)
- 決定風險管理策略
- 槓桿、倉位大小、冷卻時間等

### 4. 市場狀態卡片 (Regime Cards)
- 定義市場狀態識別規則
- 趨勢、震盪、高波動等

## 卡片選擇邏輯

```
市場狀態分析 → 選擇匹配的卡片組合 → 動態應用參數
     ↓
  實時監控 → 表現不佳時自動切換卡片
```

## 檔案結構

```
config/strategy_cards/
├── README.md           # 本文件
├── master_config.json  # 主配置 (啟用的卡片組合)
├── entry/              # 進場卡片
│   ├── aggressive.json      # 激進進場
│   ├── conservative.json    # 保守進場
│   ├── six_dim_strict.json  # 六維嚴格
│   └── scalp_fast.json      # 剝頭皮快進
├── exit/               # 出場卡片
│   ├── lock_profit.json     # N%鎖N%
│   ├── trailing_stop.json   # 追蹤止盈
│   ├── quick_scalp.json     # 快速止盈
│   └── hold_winner.json     # 持有贏家
├── risk/               # 風控卡片
│   ├── high_leverage.json   # 高槓桿
│   ├── low_risk.json        # 低風險
│   └── adaptive.json        # 自適應
└── regime/             # 市場狀態卡片
    ├── trending.json        # 趨勢市場
    ├── ranging.json         # 震盪市場
    ├── volatile.json        # 高波動
    └── calm.json            # 平靜市場
```

## 使用方式

```python
# 手動切換卡片
trader.load_card("entry", "conservative")

# 自動選擇最佳卡片
trader.auto_select_cards()

# 查看當前卡片組合
trader.show_active_cards()
```
